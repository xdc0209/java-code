判断virgo是否启动ok：目前唯一可行的方案是：搜索virgo目录下的所有plan，再通过jmx查询Active状态的plan格数进行比较是否相等。
另外，不能通过查看jmx下是否全部plan都为active状态认定virgo已成功启动，因未启动成功的plan可能会在jmx注销掉。
也不能通过判断下面的MBean的属性状态判断org.eclipse.virgo.kernel:type=KernelStatus，这个状态只表示virgo内核是否已成功启动。
