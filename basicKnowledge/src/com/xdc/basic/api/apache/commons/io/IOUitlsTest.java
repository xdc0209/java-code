package com.xdc.basic.api.apache.commons.io;

public class IOUitlsTest
{
    public static void main(String[] args)
    {
        // 使用时在具体研究，大部分方法和FileUtils重复

        // IOUtils.read(input, buffer, offset, length);
        // IOUtils.write(data, output);
        //
        // IOUtils.readLines(input, encoding);
        // IOUtils.writeLines(lines, lineEnding, output, encoding);
        //
        // IOUtils.toByteArray(input);
        //
        // IOUtils.closeQuietly(input);
    }
}
